{%- comment -%}
  Temporary placeholder for the homepage split (§9 build order, milestone 4).
  Remove once sections/world-split.liquid (or equivalent) lands.
{%- endcomment -%}
<div class="scaffold-notice">
  <p class="t-label">{{ shop.name }}</p>
  <p class="t-body">Theme scaffold — the homepage split is being built next. See the PDP for BLOSSOM and MASC in the meantime.</p>
</div>

<style>
  .scaffold-notice {
    display: grid;
    place-items: center;
    gap: var(--space-2);
    min-height: 60vh;
    text-align: center;
    padding: var(--space-4);
  }
</style>

{% schema %}
{
  "name": "Scaffold notice",
  "settings": []
}
{% endschema %}
